# H-009: Month-End Proximity / High-Volatility Interaction on Reversion

## STATUS: KILLED (2026-08-04)

### Results (pooled TRAIN, 100 signed-null seeds, top ATR tercile reused from H-005, 1H, 3-bar primary window)

| Statistic | Value |
|---|---|
| n (near-month-end) | 11,769 |
| n (ordinary) | 106,413 |
| Observed diff (primary) | -0.0070 |
| p-value | 0.8375 |
| Seed dispersion (near-ME / ordinary) | 0.2650 / 0.0942 |
| Effect exceeds seed noise? | No |
| Reverts (not persists)? | **No** -- negative sign indicates continuation dominates near month-end, opposite of the mechanism's predicted direction |

**Kill criteria triggered (multiple, independently sufficient):** the
primary result is not significant (p=0.8375, nowhere close to 0.05),
the effect does not exceed seed-to-seed dispersion, AND the sign is
wrong for the mechanism -- refutation clause 2 (must revert, not
persist) is triggered on its own regardless of significance. This is
an unambiguous kill, not a borderline dispersion call like H-008's
GBPJPY.

**Non-adjudicating side results, reported per the memo's own
transparency commitment (cannot rescue this verdict -- explicit
asymmetry, memo amendment `140ec71`):**
- Quarter-end margin: quarter-end near-ME diff (+0.0093, n=3291) vs.
  ordinary-month near-ME diff (-0.0134, n=8478) -- quarter-end IS
  directionally stronger than ordinary month-end, consistent with the
  pre-committed prediction. Irrelevant to the verdict given the primary
  failed outright, but recorded per the memo's commitment not to
  quietly drop this margin from the writeup regardless of outcome.
- Low/mid-tercile arm: observed diff +0.0168, p=0.2745 (not
  significant on its own either). Not evaluated for the undercut
  criterion since the primary never survived to be undercut -- the
  code enforces this asymmetry directly (see `run_h009.py`'s
  `compute_verdict`), not just the memo's prose.

**The mechanism as stated in `MECHANISM-MEMO-H009.md` is REFUTED.**
Month-end/quarter-end rebalancing flow does not produce a detectable,
state-dependent reversion signature in this universe at 1H, at least
not via the top-ATR-tercile / 2-business-day-window / 3-bar-reversion
operationalization tested here. No attempt was made to widen the
near-month-end window, adjust the tercile cut, or otherwise rescue the
finding after seeing this result.

**Note on the wrong-sign observation, pre-answering a future
temptation:** the negative `observed_diff` (continuation dominating
near month-end) is a residual OBSERVATION from this kill, not a new
finding and not a registered claim. It is exactly the kind of residue
that could tempt a post-hoc "month-end momentum flow" story -- the
mirror-image of the mechanism just refuted here. If anyone tests that
mirror hypothesis in the future, it requires a fresh mechanism memo
from scratch, and that memo MUST disclose that its sign was noticed in
H-009's own kill data on this TRAIN window -- this is a materially
weaker starting position than a blind registration (the TRAIN data has
already been seen for this variable), and the memo has to say so
plainly rather than present the prediction as if it were arrived at
independently.

**Engineering note:** a JSON-serialization bug (numpy bool/float types
not natively serializable) crashed the FIRST `--verdict` run after all
statistics had already printed to console -- caught immediately (the
printed numbers were legitimate; only the artifact-saving step failed),
fixed by casting to native Python types before `json.dump`, rerun
confirmed byte-identical numbers (same RNG seed, deterministic). Not a
statistical bug -- disclosed for completeness per project convention,
not because it affected the result.

Full result detail: `research/H-009-verdict.json`.

This is the ninth hypothesis overall, and Batch 2 / Phase 4's second
adjudication: **0 survivors of 8-12 budgeted, 2 hypotheses run, plus 1
documented no-candidate** (`NO-CANDIDATE-daydow-x-volatility.md`), per
the stopping rule (`research/S1_STOPPING_RULE.md`).

---

## STATUS (superseded): REGISTERED, NOT YET ADJUDICATED

Per `research/registry/MECHANISM-MEMO-H009.md` (committed `a75e315`,
amended `140ec71` to add the non-adjudicating low/mid-tercile arm), and
`research/CONDITIONAL_SEARCH_CHARTER.md`'s sequencing rule. No
reversion/outcome statistic has been computed for this hypothesis as of
this registration -- only denominators (occupancy counts) were examined
during memo drafting, per the memo's own Section 6.

**This is Batch 2 / Phase 4's second registered hypothesis** (H-008 was
the first, adjudicated KILLED).

## 1. Mechanism reference

Full economic story, counterparty argument (informationless
rebalancing/fixing-adjacent flow), and prior are in the memo, not
repeated here. Summary: near-month-end flow produces a larger, more
visible, REVERTING price displacement specifically when it coincides
with an already-thin/high-volatility state, versus limited lasting
impact under normal depth conditions.

## 2. Prediction (falsifiable form)

**Primary/adjudicating cell:** near-month-end (`bars_to_month_end <= 1`,
`research/calendar_distance.py`) x top-ATR-tercile bars (H-005's frozen
edges, `H-005-bins.json`), pooled across all 5 instruments -- no
per-instrument asymmetry predicted (unlike H-008; this mechanism has no
instrument-class split). Reversion metric identical to H-008 Section
3c, k=3 primary / k=1 secondary-descriptive.

**Refutation clause 1 (interaction, not marginal) -- REQUIRES the
low/mid-tercile arm below to be interpretable, not just the primary:**
if reversion appears equally across all volatility regimes near
month-end, the interaction claim is refuted even if a marginal
calendar effect exists.

**Refutation clause 2 (persistence check):** if the near-month-end x
high-vol effect does NOT revert (continues in the same direction as
the triggering bar), that refutes the informationless-flow counterparty
story even if some effect exists.

**Quarter-end margin (non-adjudicating, pre-committed):** quarter-ends
(Mar/Jun/Sep/Dec) predicted to show a stronger effect than ordinary
month-ends. A reversed distinction is a disclosed embarrassment for the
mechanism, not silently dropped.

**Low/mid-tercile arm (non-adjudicating, added by memo amendment
`140ec71`):** near-month-end vs. ordinary, restricted to ATR terciles
1-2. Interpretation frozen: undercuts the interaction reading (does NOT
formally kill) if and only if its effect magnitude is >=50% of the
primary's, same sign. **Asymmetric: can only undercut a POSITIVE
primary result, cannot rescue a negative one.** Routed through the
identical `permutation_test`/`seed_dispersion` path as the primary via
`build_cell_calendar`, no lower-rigor treatment, but no FDR/BH slot.

## 3. Cells and FDR-by-cell accounting

**1 primary adjudicating cell** (pooled across instruments, not
per-instrument like H-008 -- this mechanism predicts no instrument
asymmetry, so pooling is the correct operationalization of the claim,
not an FDR-budget convenience). BH correction is therefore trivial
here (m=1) -- stated explicitly so it isn't mistaken for an omitted
step. The quarter-end margin and low/mid arm are both non-adjudicating
and outside the BH accounting entirely, per Section 2.

## 4. Kill criteria (any one refutes the primary)

- Primary cell fails sufficient-n (<30 either side), fails to exceed
  seed dispersion, or fails significance -> KILLED. The low/mid arm
  CANNOT rescue this outcome (Section 2's stated asymmetry).
- Primary cell effect does not revert at k=3 (continues same-signed)
  -> KILLED (refutation clause 2), regardless of significance.
- If the primary survives all of the above, the low/mid arm is then
  consulted: if it meets the >=50%-same-sign criterion, the survival is
  reported as UNDERCUT (interaction reading not supported, plain
  marginal effect cannot be ruled out) rather than a clean survival --
  this is a narrative qualifier on a survival, not a separate kill path.

## 5. Occupancy (denominators only, verified before registration)

Primary cell (near-ME x top-tercile x quarter-end-only, the smallest/
binding sub-cell): USDJPY 732, XAUUSD 482 (smallest), GBPJPY 619,
EURUSD 784, AUDUSD 692 -- all comfortably above 30. Low/mid arm:
4123-4685 events per instrument. No power concern for either arm.

## 6. Acceptance caveat (carried forward, unconditional)

**Valid for a KILL verdict only, under the current cost model.** Cost
model v2 and AUDUSD real contract specs remain NOT DONE
(`docs/PROJECT_STATE.md` §7). Any primary survival CANNOT be accepted
until both land and the effect is re-verified under them -- per
`RESEARCH_PROGRAM.md` §6, unconditionally.

## 7. Provenance

- Memo: `research/registry/MECHANISM-MEMO-H009.md`, `a75e315` +
  amendment `140ec71`.
- ATR tercile edges reused from `research/registry/H-005-bins.json`
  (unchanged from H-008's use).
- Calendar-distance derivation: `research/calendar_distance.py`,
  validated by `tests/test_calendar_distance.py` (12 known-answer
  tests) before any occupancy check.
- Harness: `research/interaction_harness.py`'s `build_cell_calendar`,
  validated by `tests/test_interaction_harness.py`, sharing the
  identical `permutation_test`/`seed_dispersion` path as H-008's
  `build_cell` (confirmed at pre-freeze review, not merely asserted).
- This registration's commit hash is the timestamp proof that the
  primary cell, both refutation clauses, the quarter-end margin, the
  low/mid arm and its interpretation criterion, and the kill criteria
  all predate any computed statistic.
- Next step: freeze manifest, then `--verdict`.
